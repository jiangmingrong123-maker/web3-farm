/** Simulation tick — lower = smoother but heavier */
export const TD_TICK_MS = 120;

/** Gap between enemy spawns within a wave */
export const TD_SPAWN_INTERVAL_MS = 1600;

/** Countdown after clicking「开始战斗」before wave 1 */
export const TD_WAVE_PREP_MS = 3500;

/** Pause between auto waves */
export const TD_INTER_WAVE_MS = 3000;

/** Brief beat before wave 2+ spawns */
export const TD_SUB_WAVE_PREP_MS = 1000;

/** Enemy `speed` in units.ts = cells per second along the path */
