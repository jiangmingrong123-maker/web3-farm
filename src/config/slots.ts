/** Slot unlock costs — TBD, adjustable via admin / DB later. */
export const SLOT_UNLOCK_COSTS: Record<number, number> = {
  1: 0,
  2: 300,
  3: 900,
  4: 2400,
  5: 6000,
};

export const MAX_SLOTS = 5;
